import { ConfActions } from "../node/NodeConf"
import { ServiceBase } from "../ServiceBase"


// test("set state", async () => {
// 	const node = new ServiceBase("base")
// 	node.dispatch({ type: ConfActions.START, payload: { status: STATUS.START } })
// 	expect(node.state.status).toBe(STATUS.START)
// 	expect(node.name).toBe("base")
// })

